// TODO: Add more categories
const EXPENSE_CATEGORY = {
    HOME: 'home'
}

const expenseCategories = Object.values(EXPENSE_CATEGORY) 

module.exports = {
    EXPENSE_CATEGORY,
    expenseCategories
}